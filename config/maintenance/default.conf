server {
    listen 80;
    server_name localhost;

    error_page 503 /index.html;

    location / {
        return 503;
    }

    location = /index.html {
        root /usr/share/nginx/html;
        internal;
    }
}
